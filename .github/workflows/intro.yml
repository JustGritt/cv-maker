name: Intro to GitHub Actions

on: 
  push: 

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Install dependencies
        run: bun install
      - name: build
        run: bun build
      - name: run the server
        run: |
          bun run preview &
          sleep 5
          curl http://localhost:4173


